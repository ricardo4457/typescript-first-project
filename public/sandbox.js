"use strict";
// let boas: Function;
// 1
let boas;
boas = (name, numID) => {
    console.log(`${name} says ${numID}`);
};
// 2
let calc;
calc: (numbOne, numbTwo, action) => {
    if (action = 'add') {
        return numbOne + numbTwo;
    }
    else {
        return numbOne - numbTwo;
    }
};
// 3
let logDetails;
logDetails = (person) => {
    console.log(`${person.name} is ${person.age} years old`);
};
